machine:
  environment:
    PATH: "$HOME/.asdf/bin:$HOME/.asdf/shims:$PATH"

dependencies:
  cache_directories:
    - ~/.asdf
    - deps
    - _build
  pre:
    - ./circle_pre_build.sh
    - MIX_ENV=test mix deps.compile

test:
  override:
    - mix test
